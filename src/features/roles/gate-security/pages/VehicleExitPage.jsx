import PlaceholderPage from '../../../../components/common/PlaceholderPage';

export default function VehicleExitPage() {
  return <PlaceholderPage title="Vehicle Exit" subtitle="Validate delivery, gate pass, and final handover details." />;
}

