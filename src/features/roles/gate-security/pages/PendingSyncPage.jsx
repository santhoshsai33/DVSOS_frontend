import PlaceholderPage from '../../../../components/common/PlaceholderPage';

export default function PendingSyncPage() {
  return <PlaceholderPage title="Pending Sync" subtitle="Review offline PWA gate entries waiting to sync." />;
}

